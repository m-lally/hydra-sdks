[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "hydra-sdk"
version = "0.1.0"
description = "Hydra payments SDK for Python"
requires-python = ">=3.8"
license = {text = "MIT"}
