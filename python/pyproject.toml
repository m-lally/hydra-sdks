[project]
name = "hydra-payments"
version = "1.0.0"
description = "Python SDK for Hydra Payment Service"
requires-python = ">=3.10"
dependencies = ["httpx>=0.26.0"]

[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"